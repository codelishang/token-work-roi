name: update-pricing

on:
  workflow_dispatch:
    inputs:
      target_branch:
        description: Branch to refresh
        required: false
        default: main
        type: choice
        options:
          - main
          - test
  schedule:
    # GitHub cron uses UTC; this is Monday 00:01 in Asia/Shanghai.
    - cron: '1 16 * * 0'

permissions:
  contents: write

jobs:
  update-pricing-main:
    name: Refresh official model pricing (main)
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || inputs.target_branch == 'main'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: npm

      - run: npm ci --registry=https://registry.npmmirror.com
      - run: npm run typecheck:tools

      - name: Refresh official pricing
        run: npm run pricing:update

      - run: npm test

      - run: npm run build

      - run: git diff --check

      - name: Commit pricing updates
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "chore: refresh official model pricing"
          file_pattern: "src/pricing.ts data/official-pricing.json"

  update-pricing-test:
    name: Refresh official model pricing (test)
    needs: update-pricing-main
    if: >-
      always() &&
      ((github.event_name == 'schedule' && needs.update-pricing-main.result == 'success') ||
      (github.event_name == 'workflow_dispatch' && inputs.target_branch == 'test'))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test

      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: npm

      - name: Fetch pricing committed on main
        if: github.event_name == 'schedule'
        run: git fetch origin main --depth=1

      - name: Use the pricing files verified on main
        if: github.event_name == 'schedule'
        run: git checkout FETCH_HEAD -- src/pricing.ts data/official-pricing.json

      - run: npm ci --registry=https://registry.npmmirror.com
      - run: npm run typecheck:tools

      - name: Refresh official pricing
        if: github.event_name == 'workflow_dispatch'
        run: npm run pricing:update

      - run: npm test

      - run: npm run build

      - run: git diff --check

      - name: Commit pricing updates
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "chore: refresh official model pricing"
          file_pattern: "src/pricing.ts data/official-pricing.json"
